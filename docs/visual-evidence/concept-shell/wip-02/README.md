# Concept shell WIP 02

Revised actual-running-client checkpoint evidence for issue #274 after WIP 01 direction rejection. This is a direction-review WIP, not the complete issue approval packet.

The 1440×900 frame gives the approved Shuttergate environment ownership of the game composition. A compact title crest, the single visible destination title, one dominant `Begin preparation` action, deliberate Company/Forge/Settings navigation, and a portrait-led roster/resource panel are layered into reserved edge space. Player mode exposes no developer-inspection chrome, stable IDs, key/value destination rows, duplicated destination caption, or below-fold document flow.

`checkpoint-1440x900.json` binds the capture to source head `34ce2987ca23a3d703b463a07802bf07be8f47b8`, Chromium image `mcr.microsoft.com/playwright:v1.61.1-noble`, viewport, phase, required controls, player text, hidden diagnostics, stable-ID absence, scroll bounds, and screenshot SHA-256. `scripts/capture-concept-shell-checkpoint.mjs` deletes stale outputs before capture and fails on those state mismatches.

The packet intentionally stops at the one revised checkpoint frame requested by product-owner direction. It does not extend this treatment to roster/forge detail, settings, preparation, results, recovery, or mobile states before checkpoint direction review. No generated art is introduced; it uses the approved Shuttergate environment and existing production Iron Warden portrait exports.
