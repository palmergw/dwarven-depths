{
  "name": "dwarven-depths",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm build && vitest run",
    "test:built": "vitest run",
    "test:browser": "pnpm build && vitest run --config vitest.browser.config.ts",
    "test:browser:docker": "./scripts/test-browser-docker.sh",
    "lint": "biome check .",
    "format:check": "biome format .",
    "check:artifacts": "node scripts/check-generated-artifacts.mjs",
    "check:web-budgets": "node scripts/check-web-release-budgets.mjs",
    "check:desktop-package": "node scripts/check-desktop-package.mjs",
    "check:mobile-package": "node scripts/check-mobile-package.mjs",
    "build:desktop:docker": "./scripts/build-desktop-docker.sh",
    "build:mobile:docker": "./scripts/build-mobile-docker.sh",
    "test:web-offline": "node scripts/test-web-offline.mjs",
    "sim": "pnpm build && node apps/sim-cli/dist/cli.js",
    "sim:built": "node apps/sim-cli/dist/cli.js",
    "validate": "pnpm sim validate --content content/fixtures/empty-content.json --scenario scenarios/conformance/empty-level.json",
    "validate:built": "pnpm sim:built validate --content content/fixtures/empty-content.json --scenario scenarios/conformance/empty-level.json",
    "verify:scenario": "pnpm sim run --content content/fixtures/empty-content.json --scenario scenarios/conformance/empty-level.json --out .ddh/verification/empty --replace true && node apps/sim-cli/dist/cli.js replay --run .ddh/verification/empty --verify && node apps/sim-cli/dist/cli.js inspect --run .ddh/verification/empty --tick 0 --before 0 --after 0",
    "verify:scenario:built": "pnpm sim:built run --content content/fixtures/empty-content.json --scenario scenarios/conformance/empty-level.json --out .ddh/verification/empty --replace true && node apps/sim-cli/dist/cli.js replay --run .ddh/verification/empty --verify && node apps/sim-cli/dist/cli.js inspect --run .ddh/verification/empty --tick 0 --before 0 --after 0",
    "report:release-candidate": "pnpm --filter @dwarven-depths/sim-cli^... build && pnpm --filter @dwarven-depths/sim-cli build && node scripts/generate-release-candidate-reports.mjs --out .ddh/release-candidate-reports",
    "verify": "pnpm lint && pnpm check:artifacts && pnpm check:desktop-package && pnpm check:mobile-package && pnpm typecheck && pnpm build && pnpm check:web-budgets && pnpm test:web-offline && pnpm test:built && DD_SKIP_BUILD=1 pnpm test:browser:docker && pnpm validate:built && pnpm verify:scenario:built"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^22.19.11",
    "@vitest/browser-playwright": "^4.1.10",
    "playwright": "^1.61.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
