{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core window lifecycle only; gameplay and persistence remain in the existing web client.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
